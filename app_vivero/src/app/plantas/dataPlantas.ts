import { Plantas } from "./plantas";

export class plantas {
    id: string;
    type: string;
    plantas: Array<Planta>;

    constructor(id: string, type: string, movies: Array<Planta>){
        this.id = id;
        this.type = type;
        this.plantas = this.plantas;
    }
}